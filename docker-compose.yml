services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_KEY=base64:st2FXQB17Q9BmQ1YSRVAF7qh02lXXPHCoFxf5AIfgYg=
      - APP_URL=http://localhost:3001
      - PORT=3001
      - LOG_CHANNEL=stack
      - SESSION_DRIVER=file
      - CACHE_STORE=file
      - DB_CONNECTION=mysql
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_DATABASE=service_a
      - DB_USERNAME=service_a
      - DB_PASSWORD=service_a_secret
      - IAE_INTERNAL_KEY=102022400143
      - IAE_API_KEYS=102022400143
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - service-a-net

  mysql:
    image: mysql:8.0
    restart: unless-stopped
    ports:
      - "3308:3306"
    environment:
      - MYSQL_DATABASE=service_a
      - MYSQL_USER=service_a
      - MYSQL_PASSWORD=service_a_secret
      - MYSQL_ROOT_PASSWORD=root_secret
    volumes:
      - service-a-mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot_secret"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - service-a-net

volumes:
  service-a-mysql-data:

networks:
  service-a-net:
    driver: bridge
